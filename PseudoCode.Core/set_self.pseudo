DECLARE a : SET OF INTEGER
DECLARE b : SET OF INTEGER

a <- {1, 2, 3, 4, 5}
b <- {4, 5, 6, 7, 8}

OUTPUT a, b

CALL SET_UNION(a, b)
OUTPUT "UNION", a, b

CALL SET_DIFFERENCE(a, b)
OUTPUT "DIFF", a, b

CALL SET_UNION(a, {4, 5})
CALL SET_SYMMETRIC_DIFFERENCE(a, b)
OUTPUT "SYMDIFF", a, b

CALL SET_REMOVE(a, 3)
OUTPUT "REMOVE 3", a

DECLARE item : INTEGER
OUTPUT SET_TRY_GET(a, 2, item)
OUTPUT item