a <- 1 - 2 - 3 + 3 * 4 ^ 5 ^ (6 + 7)
b <- 2 > 6
CONSTANT const = 2
OUTPUT "Hi"
DECLARE arr : ARRAY[1:3, 2:5] OF STRING
arr <- [[1, 2, 3, 4], [2, 3, 4, 5], [3, 4, 5, 6]]
IF a = 1 OR b = 1 + 2 AND (NOT c AND d)
  THEN
    a <- arr[0] + b.c.d[e].f(g).h
ENDIF
IF b = 2
  THEN
    b <- 3
  ELSE
    IF b = 3
      THEN
        b <- 4
    ENDIF
ENDIF

WHILE a = 0 DO
  a <- a + 1
ENDWHILE

REPEAT
  a <- a + b
UNTIL a = b

FOR i <- 1 TO 3
  a <- a + i
NEXT i

FUNCTION sum(a: INTEGER, b: REAL) RETURNS REAL
  RETURN a + b
ENDFUNCTION
PROCEDURE printSomething
  PRINT "Hi"
ENDPROCEDURE

TYPE Test
    DECLARE d : ARRAY[1:3] OF INTEGER
    DECLARE e : REAL
ENDTYPE

DECLARE test : Test
test.d <- 1
test.e <- 2

CASE a OF
  1: PRINT "Me!"
  2:
    IF b = 3
      THEN
        INPUT This
    ENDIF
  3: a <- 4
  OTHERWISE: OUTPUT "Nonsense!"
ENDCASE


c <- 2

